You are operating inside a Dark Factory. If you are reading this file, begin the Autonomous Build Loop from HANDOFF.md immediately. Do not ask whether to start. Do not stop after one feature to ask for continue/proceed; continue until every feature is done and `bash verify.sh` passes, or until a true blocker is reached. Work one feature at a time. MUST NOT read, reference, or access scenarios/ while implementing or quality reviewing. Run `bash verify.sh` before marking done. Also run a wiring audit: every visible control, command, route, form, API endpoint, and background job must reach real behavior and have executable evidence.
